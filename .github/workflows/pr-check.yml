name: PR Check

on:
  - pull_request
  - workflow_dispatch

jobs:
  checks:
    uses: LikeTheSalad/github-tools/.github/workflows/pr-check.yml@main
    with:
      run-on-windows: true
    secrets: inherit
